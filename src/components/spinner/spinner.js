import React from 'react';

import './spinner.css';

const Spinner = () => {
    return (
        <div className="lds-css">
            <h2>loading...</h2>
        </div>
    );
};

export default Spinner;